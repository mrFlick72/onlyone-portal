apiVersion: v1
kind: Service
metadata:
  name: {{ include "budget-api.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "budget-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 8080
      name: http
  selector:
    {{- include "budget-api.selectorLabels" . | nindent 4 }}