apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "account-api.fullname" . }}-test-connection"
  labels:
    {{- include "account-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "account-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
